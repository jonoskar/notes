# Install Node.js

Install node with homebrew

# Update Node.js and npm

Update brew

First, update homebrew

`brew update`

Update node.js

`brew upgrade node`

Update npm

`npm install -g npm@latest`